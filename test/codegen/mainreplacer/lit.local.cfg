import lit
config.name = 'MainReplacerPass'

config.substitutions.append(('%check', f"""
clang++ %s -emit-llvm -Xclang -disable-llvm-passes -S -O0 -std=c++20 -o -
| opt -load-pass-plugin={config.build_pass_root}/libYieldPass.so
-passes=mainreplacer -verify-each -print-changed -stop-after=verify -S
| FileCheck -vv %s"""))
