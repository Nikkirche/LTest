import lit

config.name = 'Codegen'

config.suffixes = ['.cpp', 'h']

config.test_format = lit.formats.ShTest()
config.test_exec_root = "${CMAKE_CURRENT_BINARY_DIR}"
config.test_source_root = "${CMAKE_CURRENT_SOURCE_DIR}"
config.build_pass_root ="${CMAKE_BINARY_DIR}/codegen"
