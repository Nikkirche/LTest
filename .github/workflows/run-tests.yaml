name: run-tests
on:
  workflow_call:
jobs:
  #TODO: make a seperate build job to build only one time
  unit-tests:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    container: 
      image: silkeh/clang:19
      options: --user root
    timeout-minutes: 10
    steps:
      - name: Install deps
        run: apt update && apt install -y git ninja-build valgrind libboost-context-dev libgflags-dev libclang-19-dev
      - name: Check out repository code
        uses: actions/checkout@v4
      - name: Build
        run: |
          cmake -G Ninja -B build -DCMAKE_BUILD_TYPE=RelWithAssert
          cmake --build build --target lin_check_test
      - name: Run lin check test
        run: ctest --test-dir build -R "^LinearizabilityCheckerCounterTest" -V
  verifying-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    container: 
      image: silkeh/clang:19
      options: --user root
    timeout-minutes: 30
    steps:
      - name: Install deps
        run: |
          apt update && apt install -y git ninja-build valgrind libgoogle-glog-dev libsnappy-dev protobuf-compiler libboost-context-dev pkg-config libcapstone-dev libclang-19-dev && \
          git clone https://github.com/Kirillog/syscall_intercept.git &&  \
          cmake syscall_intercept -G Ninja -B syscall_intercept/build -DCMAKE_INSTALL_PREFIX=/usr -DCMAKE_BUILD_TYPE=Release -DCMAKE_C_COMPILER=clang && \
          cmake --build syscall_intercept/build --target install
      - name: Check out repository code
        uses: actions/checkout@v4
      - name: Build
        run: |
          cmake -G Ninja -B build -DCMAKE_BUILD_TYPE=RelWithAssert
          cmake --build build --target verify-targets verify-blocking verify-dual
      - name: "Verifying tests"
        run: ctest --parallel 4 --test-dir build -L "verify" -V
  verifying-dual-libcoro:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    container:
      image: silkeh/clang:19
      options: --user root
    timeout-minutes: 30
    steps:
      - name: Install deps
        run: apt update && apt install -y git ninja-build valgrind libboost-context-dev libgflags-dev libclang-19-dev
      - name: Check out repository code
        uses: actions/checkout@v4
      - name: Install libcoro
        run: git clone --depth=1 https://github.com/jbaldwin/libcoro.git /tmp/libcoro
      - name: Run libcoro dual matrix
        env:
          BUILD_DIR: ${{ github.workspace }}/build-dual-libcoro
        run: ./scripts/run_dual_libcoro_matrix.sh -V
  verifying-dual-folly:
    runs-on: ubuntu-latest
    env:
      LTEST_BUILD_PATH: ${{ github.workspace }}/build-dual-folly
    defaults:
      run:
        shell: bash
    container:
      image: silkeh/clang:19
      options: --user root
    timeout-minutes: 40
    steps:
      - name: Install deps
        run: |
          apt update && apt install -y git ninja-build valgrind libgoogle-glog-dev libsnappy-dev libclang-19-dev \
                    protobuf-compiler libboost-context-dev pkg-config libcapstone-dev \
                    libboost-filesystem-dev libboost-program-options-dev \
                    libboost-regex-dev libdouble-conversion-dev libfast-float-dev libevent-dev \
                    libssl-dev libfmt-dev libgoogle-glog-dev zlib1g-dev
      - name: Check out repository code
        uses: actions/checkout@v4
      - name: Build folly ltest dependencies
        run: |
          cmake -G Ninja -B "${LTEST_BUILD_PATH}" -DCMAKE_BUILD_TYPE=Release
          cmake --build "${LTEST_BUILD_PATH}" --target plugin_pass runtime
      - name: Install folly
        run: |
          git clone --depth=1 https://github.com/Kirillog/folly.git /tmp/folly
          cmake /tmp/folly -G Ninja -B /tmp/folly-build \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_CXX_STANDARD=20 \
            -DCMAKE_CXX_STANDARD_REQUIRED=ON
          cmake --build /tmp/folly-build --target install
      - name: Configure folly dual tests
        run: cmake -G Ninja -B "${LTEST_BUILD_PATH}" -DCMAKE_BUILD_TYPE=Release
      - name: Run stable folly dual matrix
        env:
          BUILD_DIR: ${{ github.workspace }}/build-dual-folly
        run: |
          ./scripts/run_dual_folly_matrix.sh -V \
            -E "verify_folly_coro_bounded_queue_|verify_folly_coro_unbounded_queue_weighted_random_t2"
  litmus-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    container:
      image: silkeh/clang:19
      options: --user root
    timeout-minutes: 30
    steps:
      - name: Install deps
        run: |
          apt update && apt install -y git ninja-build valgrind libgoogle-glog-dev libsnappy-dev protobuf-compiler libboost-context-dev pkg-config libcapstone-dev libclang-19-dev && \
          git clone https://github.com/Kirillog/syscall_intercept.git &&  \
          cmake syscall_intercept -G Ninja -B syscall_intercept/build -DCMAKE_INSTALL_PREFIX=/usr -DCMAKE_BUILD_TYPE=Release -DCMAKE_C_COMPILER=clang && \
          cmake --build syscall_intercept/build --target install
      - name: Check out repository code
        uses: actions/checkout@v4
      - name: Build
        run: |
          cmake -G Ninja -B build -DCMAKE_BUILD_TYPE=RelWithAssert
          cmake --build build --target verify-litmus
      - name: "Litmus tests"
        run: ctest --parallel 4 --test-dir build -L "litmus" -V
  verifying-folly-release:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      LTEST_BUILD_PATH: "/__w/LTest/LTest/build"
    defaults:
      run:
        shell: bash
    container: 
      image: silkeh/clang:19
      options: --user root
    steps:
      - name: Install deps
        run: |
          apt update && apt install -y git ninja-build valgrind libgoogle-glog-dev libsnappy-dev libclang-19-dev \
                    protobuf-compiler libboost-context-dev pkg-config libcapstone-dev \
                    libboost-filesystem-dev libboost-program-options-dev libboost-regex-dev \
                    libdouble-conversion-dev libfast-float-dev libevent-dev libssl-dev libfmt-dev \
                    libgoogle-glog-dev zlib1g-dev && \
          git clone --depth=1 https://github.com/Kirillog/syscall_intercept.git &&  \
          cmake syscall_intercept -G Ninja -B syscall_intercept/build -DCMAKE_INSTALL_PREFIX=/usr -DCMAKE_BUILD_TYPE=Release -DCMAKE_C_COMPILER=clang && \
          cmake --build syscall_intercept/build --target install
      - name: Check out repository code
        uses: actions/checkout@v4
      - name: Build folly ltest dependencies
        run: |
          cmake -G Ninja -B build -DCMAKE_BUILD_TYPE=Release
          cmake --build build --target plugin_pass runtime
      - name: Install folly
        run: |
          git clone --depth=1 https://github.com/Kirillog/folly.git && \
          cmake folly -G Ninja -B folly/build_dir -DCMAKE_BUILD_TYPE=Release && \
          cmake --build folly/build_dir --target install
      - name: Build tests
        run: |
          cmake -G Ninja -B build -DCMAKE_BUILD_TYPE=Release
          cmake --build build --target verifying/blocking/folly_rwspinlock verifying/blocking/folly_sharedmutex \
            verifying/blocking/folly_flatcombining_queue
      - name: Run folly tests
        run: |
          ctest --parallel 4 --test-dir build -L "^folly$" -V
